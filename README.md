# python-flask-env-builder

# Install


Just clone this project 

# Start

Run environment :

```
cd path-project
docker-compose up -d
```

then go to : `http://localhost:8000`

# Stop

To stop the docker from running :
```
docker-compose down
```